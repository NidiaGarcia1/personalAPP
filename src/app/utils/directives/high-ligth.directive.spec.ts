import { HighLigthDirective } from './high-ligth.directive';

describe('HighLigthDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLigthDirective();
    expect(directive).toBeTruthy();
  });
});
